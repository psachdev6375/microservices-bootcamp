#. Application.yml 

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/psachdev6375/microservices-bootcamp
          search-paths:
            - configuration
            
          
server:
  port: 8001