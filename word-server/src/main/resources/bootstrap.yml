spring:
  cloud:
    config:
      uri: http://192.168.0.3:8001
      fail-fast: true
      
server:
  port: ${PORT:${SERVER_PORT:0}}
     
#eureka:
#  instance:
#    metadata-map:
#      instanceId: ${spring.application.name}:${spring.application.instance_id:${random.value}}
      
---
spring:
  profiles: subject
  application:
    name: subject
---
spring:
  profiles: noun
  application:
    name: noun
---
spring:
  profiles: article
  application:
    name: article
---
spring:
  profiles: verb
  application:
    name: verb
---
spring:
  profiles: adjective
  application:
    name: adjective
---
spring:
  profiles: subject2
  application:
    name: subject
---
spring:
  profiles: noun2
  application:
    name: noun
---
spring:
  profiles: article2
  application:
    name: article
---
spring:
  profiles: verb2
  application:
    name: verb
---
spring:
  profiles: adjective2
  application:
    name: adjective
---